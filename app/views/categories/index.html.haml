.classificationArea  
  .post-new__classification
    - @posts.each do |post| 
      .post-new__classification__box
        .post-new__classification__text
          = post.text
        .classification__upper
          .post-new__classification__category
            = post.category[:name]
          .post-new__classification__area
            = post.area[:name]
        .classification__lower
          .post-new__classification__date
            = post.created_at.strftime("%Y年%m月%d日%H時%M分")
          .post-new__classification__date
            = post.user[:name]
        .post-new__classification__image
          = image_tag(post.image.to_s) if post.image?          
        = link_to '詳細', post_path(post.id), method: :get, class: "post-new__details" 